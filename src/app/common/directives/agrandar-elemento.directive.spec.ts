import { AgrandarElementoDirective } from './agrandar-elemento.directive';

describe('AgrandarElementoDirective', () => {
  it('should create an instance', () => {
    const directive = new AgrandarElementoDirective();
    expect(directive).toBeTruthy();
  });
});
