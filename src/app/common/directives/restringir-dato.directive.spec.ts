import { RestringirDatoDirective } from './restringir-dato.directive';

describe('RestringirDatoDirective', () => {
  it('should create an instance', () => {
    const directive = new RestringirDatoDirective();
    expect(directive).toBeTruthy();
  });
});
