import { DniPuntosPipe } from './dni-puntos.pipe';

describe('DniPuntosPipe', () => {
  it('create an instance', () => {
    const pipe = new DniPuntosPipe();
    expect(pipe).toBeTruthy();
  });
});
